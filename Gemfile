# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'aws-sdk-iam', '~> 1'
gem 'nokogiri' # AWS sdk needs an XML library, we went with this.
gem 'pry-byebug'
gem 'dotenv'
